services:
  - type: web
    name: templex-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python main.py --seed"
    startCommand: "uvicorn api.server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: HF_TOKEN
        sync: false
      - key: COURTLISTENER_API_TOKEN
        sync: false
      - key: CORS_ORIGINS
        sync: false
